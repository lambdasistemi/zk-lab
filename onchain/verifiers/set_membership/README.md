# set_membership — on-chain verifier skeleton

**Experimental. Toy trusted setups only.** Do not reuse anything
produced under this directory in production. Per FR-011 of
`specs/001-set-membership/spec.md`, any trusted-setup ceremony here is
for development iteration only and must be redone for real deployments.

Filled in progressively as backends are wired through the DSL in
subsequent tasks.
